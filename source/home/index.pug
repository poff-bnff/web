extends /_templates/layout.pug
include /_templates/mixins.pug

block content
    if process.env['DOMAIN'] === 'justfilm.ee'
        - var whichTypesToDisplay = ['news']
        - var heroPlusTrioNews = []

        section.grid_hero
            include /_templates/home_hero_just.pug

        section.trio_block
            include /_templates/home_trio_block_just.pug

        section.viiekas
            include /_templates/home_viiekas_just.pug

    else if process.env['DOMAIN'] === 'shorts.poff.ee'

        - var whichTypesToDisplay = ['news']
        - var heroPlusTrioNews = []

        section.grid_hero
            include /_templates/home_hero_shorts.pug

        section.trio_block
            include /_templates/home_trio_block_shorts.pug

        section.viiekas
            include /_templates/home_viiekas_shorts.pug

    else

        - var whichTypesToDisplay = ['news']
        - var heroPlusTrioNews = []

        section.grid_hero
            include /_templates/home_hero_poff.pug

        section.trio_block
            include /_templates/home_trio_block_poff.pug

        section.viiekas
            include /_templates/home_viiekas_poff.pug
