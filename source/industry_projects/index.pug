
extends /_templates/layout.pug
include /_templates/mixins.pug

block content
    section#content.schedule

        if self.data.industryprojects
            main.grid_films
                each project in self.data.industryprojects.sort(function(a, b){ if(a.title && b.title) { return ('' + a.title).localeCompare(b.title, self.locale); } else { return 0; } })
                    a.card_film(id= project.id href= `/${lang_path}${project.path}`)
                        .container_frame_16_9
                            if project.images && project.images[0]
                                img.container_contents_16_9(src= `https://assets.poff.ee/img/${project.images[0].hash}${project.images[0].ext}` alt= project.title)
                            else
                                img.container_contents_16_9(src='/assets/img/static/placeholder_hd_1.jpg' alt= project.title)

                        .card_film_title
                            h3.film_title_locale.dark_1= project.title
                                p(id= project.id+'nupp' style={color: 'red', display:'none'})
                        .card_film_info
                            .str_info_grid_3
                                - var projectTypes = []
                                each p_type in project.project_types
                                    - projectTypes.push(p_type.type)
                                if projectTypes.length
                                    .str_info_label.dark_semi= self.label.industry.projectType
                                    .str_info_value.dark_1.thin_300= projectTypes.join(', ')

            //- each project in self.data.industryprojects

            //-     if project.title && project.title.length > 1 && project.slug
            //-         a(href=`/${lang_path}${project.slug}`)
            //-             p.dark_1= project.title


            //-             if project.images && project.images.length
            //-                 each image in project.images
            //-                     img(src=`https://assets.poff.ee/img/${image.hash}${image.ext}`)
            //-             else
            //-                 img(src='/assets/img/static/placeholder_hd_1.jpg')



            //-             if project.logline
            //-                 p.dark_1= project.logline

            //-             if project.lookingFor
            //-                 p.dark_1= project.lookingFor

            //-             if project.synopsis && typeof project.synopsis === 'string'
            //-                 p.dark_1
            //-                     +format(project.synopsis)

            //-             if project.directorsNote && typeof project.directorsNote === 'string'
            //-                 p.dark_1
            //-                     +format(project.directorsNote)

            //-             if project.contactCompany
            //-                 if project.contactCompany.homepageUrl

            //-                 if project.contactCompany.type
            //-                     p.dark_1= project.type
            //-                 if project.contactCompany.name
            //-                     p.dark_1= project.contactName

            //-             if project.clipUrlCode
            //-                 p.dark_1
            //-                     +video(project.clipUrlCode)
