extends /_templates/layout.pug
include /_templates/mixins.pug



block content

    main.grid_film

        .container_frame_16_9
            if self.images
                if self.images.image
                    img.container_contents_16_9(alt= self.name ? self.name : '' data-src=`https://assets.poff.ee/img/${self.images.image.hash}${self.images.image.ext}`  src=`https://assets.poff.ee/img/${self.images.image.hash}${self.images.image.ext}`)
                else if self.images.imageDefault
                    img.container_contents_16_9(alt= self.name ? self.name : '' data-src=`https://assets.poff.ee/img/${self.images.imageDefault.hash}${self.images.imageDefault.ext}`  src=`https://assets.poff.ee/img/${self.images.imageDefault.hash}${self.images.imageDefault.ext}`)
                else
                    img.container_contents_16_9(src='/assets/img/static/placeholder_hd_1.jpg')

        if self.festival_edition && self.festival_edition.name
            br
            br
            br
            br
            h1.dark_1= self.festival_edition.name
            br
        if self.name && typeof self.name !== 'object'
            h1.dark_1= self.name
            br
        if self.description && typeof self.description !== 'object'
            h1.dark_1= self.description
            br
            br
            br
            br
            br
            br
            div.payment
                button#buybutton.btn.btn-primary(onclick=`checkIfPasswordBuyer()`) OSTA
                button#directToLoginButton.btn.btn-primary(onclick=`directToLogin(), saveUrl()`) Ostmiseks logi sisse
                button#directToaddPicture.btn.btn-primary(onclick=`directToUserProfile(), saveUrl()`) Ostmiseks lisa endast pilt
                button#directToFillProfile.btn.btn-primary(onclick=`directToUserProfile(), saveUrl()`) Ostmiseks täida profiil lõpuni
                div#paymentLinks
                br
                div.dark_1#feedback
                br
                button#paybutton.btn.btn-primary(onclick=`BuyProduct("${self.codePrefix}")`) MAKSMA
                br
    style.
        #buybutton {display: none}
        #directToaddPicture {display:none}
        #directToFillProfile {display:none}
        #paybutton {display: none}
        [type=radio]{
            position: absolute;
            opacity: 0;
            width: 0;
            height: 0;
        }
        [type=radio] + img {
            cursor: pointer;
        }
        [type=radio]:checked + img {
            outline: 4px solid #fd7e14;
        }








